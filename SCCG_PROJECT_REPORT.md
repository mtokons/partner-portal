# SCCG Partner Portal: Migration & Stabilization Report
**Date**: May 3, 2026
**Architecture**: Next.js + SharePoint Graph API + Oracle Cloud VPS

## 1. Executive Summary
The SCCG Partner Portal has been successfully migrated from a mock-reliant architecture to a high-fidelity, live-only SharePoint integration. All mock data dependencies (`USE_MOCK_DATA="false"`) have been removed. The system is now stabilized on Oracle Cloud VPS using a Docker + Caddy containerized architecture.

## 2. Core Data Architecture (The Repository)
The application follows the **Repository Pattern**, serving as the single source of truth.

### 🔑 Key SharePoint List Mappings
- **UserProfiles (UP_COL)**: Identity and specialization tracking.
- **UserRoles (UR_COL)**: Permission and role management.
- **Partners (PART_COL)**: Partner onboarding and tiers.
- **Clients (CL_COL)**: CRM/Customer data.
- **SalesOffers (SO_COL)**: Quotations and sale tracking.
- **SalesOrders (ORD_COL)**: Fulfillment and lifecycle.
- **CoinWallets (CW_COL)**: Internal ledger and balance.
- **CoinTransactions (CT_COL)**: Audit trail for all currency moves.
- **Certificates (CERT_COL)**: Credential issuance and public verification.

## 3. Implemented Modules (Phase by Phase)

### Phase 1–2: Identity & Access
- **Firebase Auth Bridge**: Linked Firebase UIDs to SharePoint `UserProfiles`.
- **Atomic Role Sync**: Implemented `getUserRoles` and `updateUserProfileRoles` with real-time propagation.

### Phase 3–4: CRM & Catalog
- **Live CRM**: Full CRUD for `Clients` and `Activities`.
- **Catalog Engine**: Implemented `ServicePackages` and `CustomerPackages`.
- **Expert Sessions**: Live expert assignment and session tracking.

### Phase 5: Revenue Engine
- **Offer-to-Order Pipeline**: Atomic creation of `SalesOffers` with multiple `SalesOfferItems`.
- **Order Fulfillment**: Automated generation of `SO-YYYY-NNNNN` numbers.

### Phase 6: Financial Ledger (The Authority)
- **SCCG Coin**: Wallet system with `ensureWallet` (auto-creation).
- **Audit Ledger**: Every balance update creates a corresponding `CoinTransaction`.

### Phase 7–8: Operations & Task Tracking
- **Kanban Board**: Drag-and-drop status updates for tasks via `updateKanbanTask`.
- **Public Verification**: Secure `/verify/[code]` route pulls live data from SharePoint.

## 4. Production Configuration
- **VPS IP**: `158.180.45.36`
- **SSL**: Automated via Caddy.
- **Deployment**: `docker-compose.yml` with `.env.production` mapping.

## 5. Relationship Details
- **Partner -> Client**: 1-to-Many relationship via `PartnerId`.
- **Offer -> Order**: 1-to-1 conversion path.
- **Order -> Item**: 1-to-Many via `SalesOrderId`.
- **Wallet -> Transaction**: 1-to-Many via `WalletId`.

## 6. Maintenance & Next Steps
1. **Backups**: Implement daily SharePoint list exports via Power Automate.
2. **Mobile**: Ready for Capacitor integration (Phase 11 logic is prepared).
3. **Analytics**: Dashboard is ready to pull from `CoinTransactions` for real-time revenue charting.

---
**Report generated by Antigravity AI.**
