import { requirePermission } from "@/lib/permissions";
import { getInvoices } from "@/lib/sharepoint";
export default async function FinanceInvoicesPage(){await requirePermission("invoice.view.all");const invoices=await getInvoices();return <div className="space-y-5"><h1 className="text-2xl font-bold">Invoices</h1><div className="overflow-x-auto border rounded-lg"><table className="w-full text-sm"><thead className="bg-muted/50 text-left"><tr><th className="p-3">Invoice</th><th className="p-3">Client</th><th className="p-3">Amount</th><th className="p-3">Due</th><th className="p-3">Status</th></tr></thead><tbody>{invoices.map(invoice=><tr key={invoice.id} className="border-t"><td className="p-3 font-medium">{invoice.invoiceNumber||invoice.id}</td><td className="p-3">{invoice.clientName||invoice.clientId}</td><td className="p-3">€{(invoice.amountEur??invoice.amount).toFixed(2)}</td><td className="p-3">{invoice.dueDate}</td><td className="p-3 capitalize">{invoice.status}</td></tr>)}</tbody></table></div></div>}