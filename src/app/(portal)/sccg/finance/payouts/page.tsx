import { requirePermission } from "@/lib/permissions";
import { getPayouts } from "@/lib/sharepoint";
export default async function FinancePayoutsPage(){await requirePermission("payout.approve");const payouts=await getPayouts();return <div className="space-y-5"><h1 className="text-2xl font-bold">Payouts</h1><div className="border rounded-lg divide-y">{payouts.map(payout=><div key={payout.id} className="p-4 flex justify-between gap-3"><div><p className="font-medium">{payout.recipientName}</p><p className="text-sm text-muted-foreground">{payout.recipientType} · {payout.relatedOrderNumber||payout.relatedOrderId}</p></div><div className="text-right"><p className="font-medium">{payout.currency} {payout.net.toFixed(2)}</p><p className="text-sm capitalize text-muted-foreground">{payout.status}</p></div></div>)}</div></div>}