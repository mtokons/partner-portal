import { requirePermission } from "@/lib/permissions";
import { getExpenses } from "@/lib/sharepoint";
import { createSccgExpenseAction } from "../actions";
export default async function FinanceExpensesPage(){await requirePermission("payment.record");const expenses=(await getExpenses()).sort((a,b)=>b.date.localeCompare(a.date));return <div className="space-y-6"><h1 className="text-2xl font-bold">Expenses</h1><form action={createSccgExpenseAction} className="grid gap-3 border rounded-lg p-5 md:grid-cols-4"><input required name="category" placeholder="Category" className="h-10 border rounded-md bg-background px-3"/><input required name="description" placeholder="Description" className="h-10 border rounded-md bg-background px-3 md:col-span-2"/><input required name="amount" type="number" min="0.01" step="0.01" placeholder="EUR amount" className="h-10 border rounded-md bg-background px-3"/><input required name="date" type="date" defaultValue={new Date().toISOString().slice(0,10)} className="h-10 border rounded-md bg-background px-3"/><button className="h-10 rounded-md bg-primary text-primary-foreground md:col-span-3">Record Expense</button></form><div className="border rounded-lg divide-y">{expenses.map(expense=><div key={expense.id} className="p-4 flex justify-between"><div><p className="font-medium">{expense.description}</p><p className="text-sm text-muted-foreground">{expense.category} · {expense.date}</p></div><p className="font-medium">€{(expense.amountEur??expense.amount).toFixed(2)}</p></div>)}</div></div>}