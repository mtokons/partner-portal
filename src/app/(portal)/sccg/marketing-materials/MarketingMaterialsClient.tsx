"use client";
import { useState, useTransition } from "react";
import { Download, Upload } from "lucide-react";
import type { Product } from "@/types";
import { uploadMarketingMaterialAction } from "./actions";
export default function MarketingMaterialsClient({ materials }: { materials: Product[] }) { const [pending, startTransition] = useTransition(); const [notice, setNotice] = useState(""); return <div className="space-y-6"><form action={(data) => startTransition(async () => { const result = await uploadMarketingMaterialAction(data); setNotice(result.success ? "Material uploaded." : result.error || "Upload failed."); if (result.success) window.location.reload(); })} className="grid gap-3 rounded-xl border border-border bg-card p-4 md:grid-cols-[1fr_2fr_1fr_auto]"><input name="title" required placeholder="Material title" className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><input name="description" placeholder="Description" className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><input name="file" type="file" required className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><button disabled={pending} className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-3 py-2 text-sm font-medium text-primary-foreground disabled:opacity-50"><Upload className="h-4 w-4" />Upload</button></form>{notice && <p className="text-sm text-muted-foreground">{notice}</p>}<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{materials.map((material) => <div key={material.id} className="rounded-xl border border-border bg-card p-4"><h2 className="font-semibold">{material.name}</h2><p className="mt-2 min-h-10 text-sm text-muted-foreground">{material.description || "No description."}</p>{material.imageUrl && <a href={material.imageUrl} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-1 text-sm text-primary hover:underline"><Download className="h-4 w-4" />Download</a>}</div>)}{materials.length === 0 && <p className="text-sm text-muted-foreground">No marketing materials yet.</p>}</div></div>; }