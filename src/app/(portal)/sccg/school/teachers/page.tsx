import { requirePermission } from "@/lib/permissions";
import { getSchoolTeachers } from "@/lib/firestore-services";
import { createTeacherAction } from "../actions";
export default async function TeachersPage(){await requirePermission("school.teacher.manage");const teachers=await getSchoolTeachers();return <div className="space-y-6 max-w-6xl mx-auto"><h1 className="text-2xl font-bold">Teachers</h1><form action={createTeacherAction} className="grid gap-3 border rounded-lg p-5 md:grid-cols-3"><input required name="name" placeholder="Full name" className="h-10 border rounded-md bg-background px-3"/><input required name="email" type="email" placeholder="Email" className="h-10 border rounded-md bg-background px-3"/><input name="phone" placeholder="Phone" className="h-10 border rounded-md bg-background px-3"/><input name="specialization" placeholder="Specialization" className="h-10 border rounded-md bg-background px-3"/><input name="language" placeholder="Language" className="h-10 border rounded-md bg-background px-3"/><input name="revenueSharePercent" type="number" min="0" max="100" placeholder="Revenue share %" className="h-10 border rounded-md bg-background px-3"/><button className="h-10 rounded-md bg-primary text-primary-foreground md:col-span-3">Add Teacher</button></form><div className="border rounded-lg divide-y">{teachers.map(teacher=><div key={teacher.id} className="p-4 flex justify-between"><div><p className="font-medium">{teacher.name}</p><p className="text-sm text-muted-foreground">{teacher.email} · {teacher.specialization}</p></div><span className="text-sm capitalize">{teacher.status}</span></div>)}</div></div>}