import Link from "next/link";
import { requirePermission } from "@/lib/permissions";
import { getSchoolCertificates, getSchoolEnrollments } from "@/lib/firestore-services";
import { issueCertificateAction, revokeCertificateAction } from "../actions";
export default async function CertificatesPage(){await requirePermission("school.certificate.issue");const[certificates,enrollments]=await Promise.all([getSchoolCertificates(),getSchoolEnrollments({status:"completed"})]);return <div className="space-y-6 max-w-7xl mx-auto"><h1 className="text-2xl font-bold">Certificates</h1><div className="border rounded-lg p-5"><h2 className="font-semibold mb-3">Eligible enrollments</h2><div className="space-y-2">{enrollments.map(enrollment=><div key={enrollment.id} className="flex items-center justify-between gap-3"><span className="text-sm">{enrollment.studentName} · {enrollment.courseName} · {enrollment.finalGrade}</span><div className="flex gap-2"><form action={issueCertificateAction.bind(null,enrollment.id,"participation")}><button className="border rounded-md px-3 py-1.5 text-xs">Participation</button></form><form action={issueCertificateAction.bind(null,enrollment.id,"completion")}><button className="bg-primary text-primary-foreground rounded-md px-3 py-1.5 text-xs">Completion</button></form></div></div>)}</div></div><div className="border rounded-lg divide-y">{certificates.map(certificate=><div key={certificate.id} className="p-4 flex flex-wrap justify-between gap-3"><div><p className="font-medium">{certificate.studentName} · {certificate.courseName}</p><p className="text-sm text-muted-foreground">{certificate.certificateNumber} · {certificate.certificateType} · {certificate.status}</p><Link href={`/verify/${certificate.verificationCode}`} className="text-xs text-primary">Public verification</Link></div>{certificate.status==="issued"&&<form action={revokeCertificateAction.bind(null,certificate.id)} className="flex gap-2"><input required name="reason" placeholder="Revocation reason" className="h-9 border rounded-md bg-background px-2 text-sm"/><button className="h-9 border border-destructive text-destructive rounded-md px-3 text-sm">Revoke</button></form>}</div>)}</div></div>}